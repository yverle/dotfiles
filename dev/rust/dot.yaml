windows:
  # installs:
  #   cmd: choco install rustup
  #   depends: ../tools/choco
  installs: winget install Rustlang.Rustup

linux|darwin:
  installs: curl https://sh.rustup.rs -sSf | sh -s -- -y
