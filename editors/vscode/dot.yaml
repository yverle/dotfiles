windows:
  links:
    keybindings.json: ~\AppData\Roaming\Code\User\keybindings.json
    settings.json: ~\AppData\Roaming\Code\User\settings.json

  depends:
    - ../terminal/pwsh
    - ../tools/git

  installs: winget install --silent --no-upgrade Microsoft.VisualStudioCode
