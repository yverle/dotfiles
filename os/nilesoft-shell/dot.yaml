# Only on Windows
windows:
  installs: winget install --silent --no-upgrade Nilesoft.Shell

  # TODO: Doesn't work anymore?
  # hooks:
  #   install:
  #     post: |
  #       shell -register -init
  #       shell -restart
  #   link:
  #     post: shell -restart

  links:
    shell.nss: 'C:\Program Files\Nilesoft Shell\shell.nss'
